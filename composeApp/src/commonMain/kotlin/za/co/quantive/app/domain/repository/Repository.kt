package za.co.quantive.app.domain.repository

/**
 * Base marker interface for repositories
 * Repositories are responsible for data access and abstraction
 * They provide a clean API for data operations to the domain layer
 */
interface Repository
